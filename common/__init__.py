"""
common — โค้ดที่ ml/ และ backend/ ใช้ร่วมกัน (single source of truth)

ฟังก์ชันทำความสะอาดข้อความ/ตัดคำต้องเหมือนกันทุกตัวอักษรทั้งตอนเทรน (ml/) และตอนใช้งานจริง
(backend/app/nlp/nlp_process.py) ไม่เช่นนั้นโมเดลจะทำนายผิดเพี้ยนแบบเงียบ ๆ จึงรวมไว้ที่นี่ที่เดียว

Backend Dockerfile จะ COPY โฟลเดอร์นี้เข้าไปใน image ด้วย จึงไม่ต้องพึ่ง path ที่ชี้ออกไปนอก backend/
"""
