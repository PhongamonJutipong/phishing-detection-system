# dependencies ของ backend — แหล่งข้อมูลจริงอยู่ใน pyproject.toml ที่ระดับ repo root
# ไฟล์นี้เก็บไว้เพื่อให้ Dockerfile ติดตั้งได้โดยไม่ต้องคัดลอกซอร์สทั้งหมดเข้ามาก่อน
# ซึ่งทำให้ Docker cache layer การติดตั้งไว้ได้
#
# ติดตั้งสำหรับพัฒนา (ได้ ml + dev ด้วย):  pip install -e ".[ml,dev]"
.
