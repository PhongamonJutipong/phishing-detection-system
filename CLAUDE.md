# บริบทของโปรเจคสำหรับ AI agent

ไฟล์นี้ถูกโหลดอัตโนมัติทุกครั้งที่เปิดเซสชัน อ่านก่อนเริ่มงาน
สำหรับมนุษย์ที่เพิ่งเข้าโปรเจค ให้อ่าน [docs/SETUP.md](docs/SETUP.md) แทน

ระบบตรวจจับอีเมลฟิชชิงด้วย NLP เป็นปริญญานิพนธ์ ประกอบด้วย ML pipeline (`ml/`),
FastAPI + PostgreSQL (`backend/`), หน้าเว็บ Angular (`frontend/`) และส่วนขยาย Chrome (`extension/`)

---

## ข้อจำกัดที่ห้ามละเมิด

**1. อัลกอริทึมคือ TF-IDF + Multinomial Naive Bayes ห้ามเปลี่ยน**

แม้ผลการทดลองจะชี้ว่า LinearSVC แม่นกว่า (0.9832 เทียบกับ 0.9679) ก็ห้ามเปลี่ยน
เพราะเอกสารบทที่ 3 เขียนอิงอัลกอริทึมนี้ไว้แล้วทั้งแผนภาพคลาสและพจนานุกรมข้อมูล

ถ้าจะเพิ่มความแม่นยำ ให้ปรับที่พารามิเตอร์ของ TF-IDF และ Naive Bayes เท่านั้น
ก่อนเสนอเทคนิคใดที่เปลี่ยนตัว classifier ต้องถามผู้ใช้ก่อนเสมอ

**2. รองรับเฉพาะภาษาไทยและอังกฤษ**

เจอเนื้อหาภาษาอื่นในโค้ด ชุดข้อมูล เทสต์ หรือเอกสาร ให้เอาออก
`ml/preprocess.py` มีตัวกรอง `_drop_other_languages()` ตัด 7 ระบบอักษรออกอัตโนมัติ

ถ้าต้องทดสอบกรณีที่โมเดลไม่รู้จักคำ ให้ใช้ตัวเลขล้วนหรือคำไม่มีความหมายในอักษรละติน
**ห้ามใช้ภาษาอื่นเป็นตัวอย่าง** (เคยพลาดมาแล้ว ต้องมาตามลบทีหลัง)

**3. ค่าเริ่มต้นด้านความเป็นส่วนตัวห้ามเปิดโดยพลการ**

`STORE_EMAIL_CONTENT` และ `STORE_NLP_ARTIFACTS` เป็น `false` โดยตั้งใจ
คอลัมน์ `tokenized_word.word` เก็บคำของอีเมลเป็นข้อความธรรมดาผูกกับ `email_id`
ใครอ่านฐานข้อมูลได้ก็ประกอบเนื้อหาอีเมลกลับได้ แม้ `body_encrypted` จะเข้ารหัสไว้
เปิดได้เฉพาะตอนเก็บข้อมูลทำวิจัยและต้องแจ้งผู้ใช้

ระบบบัญชีผู้ใช้ก็ใช้หลักเดียวกัน **ห้ามผูกผลการตรวจอีเมลกับ `user_id`** (จะรู้ว่าใครได้อีเมลแบบไหน)
และห้ามเก็บอีเมลหรือ token เป็นข้อความธรรมดา ถ้าแก้เนื้อหานโยบายความเป็นส่วนตัว
ต้องเปลี่ยนเลขฉบับให้ตรงกันทั้ง `privacy_policy_version` ใน `backend/app/config.py`
และ `POLICY_VERSION` ใน `frontend/src/app/pages/privacy.ts`

---

## ตัวเลขปัจจุบัน (อย่าคำนวณใหม่ ใช้ค่าเหล่านี้)

| | อังกฤษ | ไทย |
|---|---|---|
| ข้อมูลเทรน / ทดสอบ | 39,371 / 9,843 | 2,006 / 502 |
| 5-fold CV accuracy | 0.9678 ± 0.0015 | 0.9936 ± 0.0008 |
| recall | 0.9487 | 1.0000 |
| **ทดสอบข้ามแหล่ง** | — | **0.84 – 0.95** |

**CV ฝั่งไทย 0.99 สูงเกินจริง ห้ามรายงานลำพัง** ข้อมูลไทยทั้ง 2,508 ฉบับเป็นข้อมูลสังเคราะห์
และ `tu_scam_dataset` (2,182 ฉบับ) สร้างจากแม่แบบซ้ำ ๆ โมเดลจึงจำแม่แบบได้
ตัวเลขที่ใช้รายงานได้คือ**ทดสอบข้ามแหล่ง** (กันไว้ทีละแหล่ง เทรนด้วยที่เหลือ)
อยู่ที่ `ml/results/cross_source_th.csv` ดู [docs/decisions.md](docs/decisions.md) ข้อ 9
ข้อมูลไทยจริงยังเป็นช่องว่างที่ใหญ่ที่สุดของโครงงาน

ตารางเทียบ 5 อัลกอริทึมตามบทที่ 3.3.3 อยู่ที่ `ml/results/algorithm_comparison_{en,th}.csv`

เทสต์ปัจจุบัน **44 ข้อ** ต้องผ่านทั้งหมดก่อน commit

---

## กับดักที่เคยเสียเวลาไปแล้ว

**`sys.path` — อย่าเพิ่มกลับเข้ามา** โปรเจคติดตั้งเป็นแพ็กเกจผ่าน `pyproject.toml`
(`pip install -e ".[ml,dev]"`) จึง `import app.*` และ `import common.*` ได้จากทุกโฟลเดอร์
เดิมมี `sys.path.insert()` อยู่ 6 จุด ลบออกหมดแล้ว

**CWD สำคัญตอนรัน uvicorn เอง** `app/config.py` ใช้ `env_file=".env"` ซึ่งอิงโฟลเดอร์ปัจจุบัน
ถ้ารันจาก repo root จะหา `backend/.env` ไม่เจอ แล้ว `ENCRYPTION_KEY` กลายเป็นค่าว่าง
**โดยไม่มีข้อความเตือนชัดเจน** ต้อง `cd backend` ก่อนเสมอ (ใน container ไม่มีปัญหานี้)

**`StaticFiles` โยน exception ไม่ได้คืน response 404** การเช็ค `status_code` จึงไม่ทำงาน
`SpaStaticFiles` ใน `backend/app/main.py` ต้องดักที่ `StarletteHTTPException`

**PowerShell กับโปรแกรมภายนอก** `docker info *>$null` ทำให้สคริปต์ตายเมื่อ `$ErrorActionPreference = "Stop"`
เพราะ PowerShell ถือว่า stderr ของโปรแกรมภายนอกเป็น error เมื่อมี redirect
ดูวิธีแก้ที่ฟังก์ชัน `Test-DockerReady` ใน `scripts/setup.ps1`

**ไฟล์ `.ps1` ต้องมี UTF-8 BOM** ไม่งั้น Windows PowerShell 5.1 อ่านเป็น cp1252 แล้วภาษาไทยพาร์สไม่ผ่าน
และ `.sh` ต้องเป็น LF เสมอ (บังคับไว้ใน `.gitattributes`) ไม่งั้นคอนเทนเนอร์ Linux รันไม่ขึ้น

**Console ของ Windows พิมพ์ภาษาไทยไม่ได้** ต้องใส่ `PYTHONIOENCODING=utf-8` นำหน้าคำสั่ง Python
ที่จะพิมพ์ภาษาไทย ไม่งั้นได้ `UnicodeEncodeError: 'charmap' codec`

**อย่าใส่ path ที่มี backslash ลงในสตริง Python ที่เขียนไฟล์** `\n` `\b` `\r` `\a`
จะกลายเป็นอักขระควบคุมจริงและทำไฟล์เสีย ใช้ forward slash หรือ `chr(92)` แทน (พลาดมาแล้ว 4 ครั้ง)
ครั้งล่าสุด `\r` ใน `ml\results` ถูกแปลงเป็นขึ้นบรรทัดใหม่ตอนแก้ไฟล์รอบถัดไป สแกนหาอักขระควบคุมอย่างเดียวจึงไม่เจอ
ต้องค้นด้วย `grep` หาข้อความ path ที่ตั้งใจเขียนว่ายังอยู่ครบ

**ทดสอบ API ด้วยข้อความไทยผ่าน `curl` ใน bash ไม่ได้** shell ทำ encoding เพี้ยน
จนภาษาไทยถูกตรวจเป็น `en` ให้ยิงผ่าน Python + `json.dumps(...).encode('utf-8')` แทน

---

## โครงสร้างและคำสั่ง

```
pyproject.toml          แหล่งเดียวของ dependencies + ตั้งค่า pytest + ประกาศแพ็กเกจ
common/                 โค้ดที่ ml/ และ backend/ ใช้ร่วมกัน (ตัดคำ ตรวจภาษา)
ml/                     สคริปต์เตรียมข้อมูลและเทรน (ไม่ใช่แพ็กเกจ รันตรง ๆ)
backend/app/            แพ็กเกจ `app` — FastAPI
backend/app/web/        ผลลัพธ์ build ของ Angular (ไม่อยู่ใน git)
frontend/               Angular 22 standalone + signals + lazy loading
extension/              Chrome Extension MV3
```

```bash
pip install -e ".[ml,dev]"          # ติดตั้งครบสำหรับพัฒนา
python -m pytest                    # 44 ข้อ ต้องผ่านหมด
scripts\train.bat                   # เทรนใหม่จากข้อมูลที่ preprocess ไว้แล้ว แสดงความคืบหน้า + เก็บ log
cd frontend && npm run build        # ผลลัพธ์ไป backend/app/web
docker compose up -d --build        # ทั้งระบบ
```

preprocess ใหม่ (เช่นเพิ่มชุดข้อมูล) ต้องชี้โฟลเดอร์ชุดข้อมูลภายนอกด้วยเสมอ
**ไม่งั้น `ml/data/processed` จะถูกเขียนทับด้วยชุดจำลองเล็ก ๆ และข้อมูลอังกฤษ 49,000 ฉบับหายหมด**

```bat
scripts\train.bat --extra-dir "C:/Users/Noxivelle/Desktop/project+doc/phishing email data public"
```

---

## วิธีทำงานที่ผู้ใช้คาดหวัง

- **ตรวจด้วยการรันจริง ไม่ใช่การอ่านโค้ดแล้วสรุป** ผู้ใช้ให้ค่ากับหลักฐานที่วัดได้
- **รายงานตามจริง** ถ้าทดสอบไม่ได้ให้บอกว่าทดสอบไม่ได้ อย่าอ้างว่าผ่าน
- **แก้เฉพาะที่เกี่ยวข้องกับคำขอ** ไม่ปรับปรุงโค้ดข้างเคียงที่ไม่ได้สั่ง
- **commit และ push เมื่อผู้ใช้สั่งเท่านั้น** ก่อน commit ให้สแกนหาความลับและไฟล์ใหญ่เสมอ
- ผู้ใช้สื่อสารเป็นภาษาไทย ตอบเป็นภาษาไทย

**ห้ามออกอินเทอร์เน็ตโดยไม่ได้รับอนุญาต** ผู้ใช้อนุญาตเฉพาะการดาวน์โหลดแพ็กเกจจาก
registry สาธารณะ (npm / PyPI / Docker Hub) เท่านั้น และห้ามส่งข้อมูลของโปรเจคออกไป

---

## งานที่ยังค้าง

1. **ข้อมูลภาษาไทยจริง** — สำคัญที่สุด ตอนนี้มี 2,508 ฉบับแต่เป็นข้อมูลสังเคราะห์ทั้งหมด
   ผู้ใช้จะหามาให้เมื่อขอ
2. ระบบบัญชี (`app_user`, `user_session`) ยังไม่มีการยืนยันอีเมลและลืมรหัสผ่าน
   และ**สองตารางนี้ไม่อยู่ในพจนานุกรมข้อมูลบทที่ 3** ถ้าจะใส่ในเล่มต้องเพิ่มเอง
3. rate limit นับในโปรเซสเดียว ถ้าขยายหลาย worker ต้องย้ายไป Redis

---

## เอกสารอื่น

| ไฟล์ | เนื้อหา |
|---|---|
| [docs/SETUP.md](docs/SETUP.md) | คู่มือติดตั้งสำหรับมนุษย์ ปัญหาที่เจอบ่อย |
| [docs/decisions.md](docs/decisions.md) | บันทึกการตัดสินใจพร้อมหลักฐานที่วัดได้ |
| [docs/privacy-policy.md](docs/privacy-policy.md) | นโยบายความเป็นส่วนตัว ไทย/อังกฤษ |
| [docs/agents/](docs/agents/) | ธรรมเนียมการใช้ issue tracker และ label |
